<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
x:Class="TarefasApp.MainPage"
Title="Tarefas">


    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Adicionar" Clicked="OnAddClicked"/>
    </ContentPage.ToolbarItems>


    <CollectionView x:Name="TasksCollectionView"
ItemsSource="{Binding Tasks}"
SelectionMode="Single"
SelectionChanged="OnSelectionChanged">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Frame Margin="8" Padding="10" HasShadow="True" CornerRadius="8">
                    <StackLayout>
                        <Label Text="{Binding Title}" FontAttributes="Bold"/>
                        <Label Text="{Binding Description}" LineBreakMode="TailTruncation"/>
                    </StackLayout>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>


</ContentPage>